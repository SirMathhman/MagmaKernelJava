{
	Assertions.assertIntEquals("Length", 4, Strings.length("test"));
	Assertions.assertIntEquals("Compare", 0, Strings.compare("foo", "foo"));
	Assertions.assertTrue("Equals", Strings.equals("bar", "bar"));
	Assertions.assertStringEquals("AssertString", "bar", "bar");

	val copy = Strings.copy("test");
	Assertions.assertStringEquals("Copy", "test", copy);
	Memory.empty(copy);

	val concat = Strings.concat("foo", "bar");
	Assertions.assertStringEquals("Concat", "foobar", concat);
	Memory.empty(concat);
}